class Foo {}
class Greeter {
  hello() {}
}

"" + Foo; // expect runtime error: Runtime error: Operands must be numbers or strings for +.

// Bound methods should also not concatenate. The VM halts on the first
// runtime error, so reaching this line would raise the same message.
Greeter().hello + "";
